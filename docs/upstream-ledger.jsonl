{"date": "2026-07-08", "upstream_ref": "v0.34.0 (fork at `HEAD`, 59 commits behind / 21 commits ahead of `upstream/main` per `git rev-list --left-right --count upstream/main...HEAD`; fork `package.json` version `0.31.3` vs. latest upstream tag `v0.34.0`)", "intake_type": "release", "integration_pr": "n/a (baseline)", "status": "adopted-with-exceptions", "exceptions": [], "notes": "Baseline only — no per-commit exceptions recorded yet; see `docs/tlh-patch-inventory.md` for the deliberate fork deltas presumed to still diverge from upstream through v0.34.0. Pre-workflow baseline row seeded by `ps-f6se` when this ledger and the non-rebase intake workflow (`docs/UPSTREAM-SYNC.md`) were introduced. No intake decisions have been made under the new workflow yet — the fork has not evaluated upstream commits between the last sync point and `v0.34.0` for adoption or rejection. The next real intake should target catching up to `v0.34.0` (or a coherent subset of it) via an explicit merge/squash-import PR per §2, re-verifying every row in `docs/tlh-patch-inventory.md` (especially `src/runs/shared/pi-spawn.ts`), and replacing this baseline row's implicit \"not yet evaluated\" status with real adopted/rejected exceptions."}
